dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon
    }
}
rootProject.name = "QA"
include ':app'
// ':qrcodescanner' (com.blikoon, Camera1/ZXing) inlocuit de CameraX + ML Kit direct in :app,
// la fel ca in pms_3u354 si mm. Modulul ramane pe disc, neconstruit.
